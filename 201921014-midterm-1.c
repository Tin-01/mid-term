#include <stdio.h>


double aver-age(double in1, double in2){
	result = (double)(in1 + in2)/2;
	printf("%lf", result);
}

int main(){
	double a1, a2;
	scanf("%lf %lf",a1, a2);
	aver-age(a1,a2);
	
	return 0;

}

